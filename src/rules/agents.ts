/** Agents that receive individual per-rule files */
export const RULE_PER_FILE_AGENTS = [] as const;

export const RULE_INDIRECT_AGENTS = [] as const;

export const RULE_UNSUPPORTED_AGENTS = ['claude-desktop'] as const;
